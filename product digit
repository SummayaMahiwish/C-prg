#include<stdio.h>

//Recursive Function
int productDigits(int n);

int main(){
	
	int userInput;
	
	printf("input: ");
	scanf("%d", &userInput);
	int output = productDigits(userInput);
	
	printf("Output: %d \n", output);
	
	return 0;
}
