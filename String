#include<stdio.h>
#include<string.h>
void main()
{
char s[50];
int i,j,s,cnt=0,len,l;
puts("enter string");
gets(s);
len=strlen(s);
for(i=0;i<len;i++)
{
if(s[i]==32||s[i]==0)
{
for(j=i-1;j>cnt-1;j--)
{
printf("%c"s[j])
}
if(cnt==0)
{
printf(" ");
}
cnt=1;
}
}
}
