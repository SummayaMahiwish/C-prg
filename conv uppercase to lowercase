#include<stdio.h>
void strconv(char[]);
void main()
{
char s[500];
char upp,low;
puts("string");
gets(s);
strconc(s);
printf("after convertion");
printf(s);
}
void strconv(char *ptr)
{
for(;*ptr;ptr++)
{
if(*ptr>=65&&*ptr<=90)
*ptr+=32;
else
{
if(*ptr>=97&&*ptr<=122)
*ptr-=32;
}
}
}

